인스턴스(Instance)

객체 : 클래스를 기반으로 만들어진 모든 것을 포괄하는 용어
인스턴스 : 클래스로부터 생성된 개별 객체 (인스턴스 ⊂ 객체)
		 참조변수를 통해서만 접근 가능함

<구조>		 
	클래스명 변수명; //클래스의 객체를 참조하기 위한 참조변수 선언
	변수명 = new 클래스명(); //클래스의 객체를 생성 후, 객체의 주소를 참조변수에 저장

ex)
TV tv; //TV인스턴스를 참조하기 위해서 변수 tv선언
	TV 클래스 타입의 참조변수 tv를 선언
	메모리공간에 참조변수 tv를 위한 공간 선언 (=/=공간생성)
	인스턴스 생성되지 않은 상태이므로 참조변수로 할 수 있는 것은 없으며, 선언만 된 상태
	
tv = new TV(); //TV인스턴스를 생성한 다음, 생성된 TV인스턴스 주소를 참조변수 tv에 저장
	연산자 new에 TV클래스의 인스턴스가 메모리 빈 공간에 생성
	대입연산자(=)에 의해 생성된 객체의 주소값이 참조변수 tv에 저장
	참조변수 tv를 통해 TV 인스턴스에 접근할 수 있음
	인스턴스를 다루기 위해서 참조변수는 반드시 필요함
	
--------------------------------------------
객체: 클래스를 처음 생성했을 때만 그렇게 부르고
> 객체 만들고 어디 써먹으려고 하면 인스턴스라고 부르자!

객체-설계도 / 인스턴스-설계도에 맞춰 만들어진 어떤 존재